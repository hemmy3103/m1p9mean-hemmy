const auth = require('./auth');
const produits = require('./produits');
const restaurants = require('./restaurants');
const commandes = require('./commandes');
module.exports = {auth, produits, restaurants, commandes}